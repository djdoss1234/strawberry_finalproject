# 2026-05-28 Worklog

## SW MoveSplineJoint Silent No-Motion 대응

- 현상:
  - `root/sw` 단일 실행에서 cuRobo plan은 `PLAN_VALID`.
  - endpoint: `[149.6, 1.1, 142.6, -124.0, 93.6, 58.7]`
  - `/dsr01/motion/move_spline_joint` 응답은 `success=True`.
  - 하지만 실제 로봇이 움직이지 않아 `EXEC_TIMEOUT` 발생.
  - 동일 환경에서 `MoveJoint` overview return은 정상 동작.

- 해석:
  - planner/IK 실패가 아니라 Doosan `MoveSplineJoint`가 특정 SW joint
    spline을 조용히 수락만 하고 실제 motion을 시작하지 않는 실행 계층
    문제로 분리.
  - `MoveSplineJoint` 응답에는 `msg` 필드가 없으므로 `msg='N/A'`는
    내부 rejection 메시지가 아님.

- 조치:
  - `root/sw`에만 저속 spline probe 적용.
    - velocity: `20 deg/s`
    - minimum request time: `15 s`
  - spline 응답 후 `5 s` 동안 현재 joint가 시작점에서 `2 deg` 이내이면
    silent no-motion으로 판단.
  - silent no-motion일 때 SW endpoint로 저속 `MoveJoint` fallback 실행.
    - velocity: `15 deg/s`
    - acceleration: `20 deg/s^2`
  - fallback 후에도 기존 endpoint 도착 확인 로직을 유지.

- 검증:
  - `py_compile`: 통과
  - `pytest`: `52 passed`
  - `git diff --check`: 통과
  - `colcon build --packages-select strawberry_motion --symlink-install`: 통과

## SW Direct MoveJoint 실행 결과와 Staged 진단 전환

- 실행 결과:
  - `root/sw` direct MoveJoint endpoint test도 service response는
    `success=True`.
  - target: `[149.6, 1.1, 142.6, -124.0, 93.6, 58.7]`
  - 실제 관절 도착은 없었고 `EXEC_TIMEOUT` 발생.
  - overview return MoveJoint 역시 현재 overview 위치이므로 별도 이동
    여부 판단 근거로는 약함.
- 해석:
  - SW 최종 endpoint 또는 한 번에 보내는 큰 joint move가 Doosan controller에서
    silent reject/hold되는 것으로 의심.
- 수정:
  - `root/sw`를 cuRobo trajectory에서 샘플링한 6개 이하 coarse waypoint로
    나누어 저속 `MoveJoint`를 순차 실행하는 staged diagnostic으로 변경.
  - 각 stage target과 현재 joint를 로그로 남겨 어느 구간에서 멈추는지 확인.
- 검증:
  - `py_compile`: 통과
  - `pytest`: `52 passed`
  - `git diff --check`: 통과
  - `colcon build --packages-select strawberry_motion --symlink-install`: 통과

## 기록 보강

- SW 도달 실패는 중요한 troubleshooting case로 분리 기록했습니다.
- issue:
  `docs/issues/ISSUE-20260528-008_sw_plan_valid_execution_no_motion.md`
- run:
  `docs/runs/RUN-20260528-002_sw_execution_no_motion.md`
- 핵심 메시지:
  - cuRobo `PLAN_VALID`는 실제 controller execution success가 아니다.
  - Doosan service `success=True`도 실제 arrival success가 아니다.
  - 실제 성공 판정은 joint-state arrival check로 해야 한다.

## SW Staged MoveJoint 결과: Stage4 임시 Scan Pose 채택

- 실기 staged MoveJoint 결과:
  - stage1 도달: `[97.8, -94.4, 65.9, -10.9, 95.5, -94.8]`
  - stage2 도달: `[103.6, -82.2, 74.1, -23.2, 95.4, -79.3]`
  - stage3 도달: `[117.5, -55.5, 94.2, -53.1, 95.2, -38.5]`
  - stage4 도달: `[133.5, -26.6, 117.8, -88.0, 94.6, 10.2]`
  - stage5 target `[145.8, -5.4, 136.6, -115.5, 93.8, 47.9]`에서 no-arrival.
- 결론:
  - SW 전체가 unreachable인 것이 아니라 stage4 이후 구간에서 controller
    execution이 실패한다.
  - stage4는 안전하게 도달한 첫 임시 scan pose이며, 카메라 시야가 부족하면
    stage4와 stage5 사이를 더 촘촘히 나눠 가까운 도달 가능 pose를 찾는다.
- 수정:
  - `root/sw` staged execution은 stage4 도착 시 성공으로 처리.
  - `config/scan_pose_candidates_refit_candidate.yaml`에
    `temporary_scan_joints_deg`와 근거를 기록.

## SW Stage4 임시 Scan Pose 성공 Run

- 2026-05-28 12:23 run에서 `root/sw` stage4 임시 scan pose 도달 성공.

```text
AT_SCAN_POSE root/sw joints_deg=[133.5 -26.6 117.8 -88.1 94.6 10.3]
SCANNED_EMPTY root/sw no detection in dwell window
RETURNING_TO_OVERVIEW
AT_OVERVIEW joints_deg=[97.8 -94.4 65.9 -10.9 95.5 -94.8]
SCAN_COMPLETE
```

- 의미:
  - SW cell이 물리적으로 가까워도, 현재 고정 camera orientation과
    final endpoint 조건을 동시에 만족하려면 stage4 이후 큰 joint 자세
    전환이 필요하다.
  - 따라서 문제는 단순 거리 문제가 아니라 특정 joint-space 구간의
    controller execution validity 문제로 정리한다.
- 다음:
  - 사용자가 “그냥 카메라 가까이”를 우선 요구했으므로, stage4 고정 대신
    SW trajectory를 10개 waypoint로 촘촘히 나눠 stage7까지 더 접근한다.
  - stage7 전에 no-arrival이 발생하면 마지막 실제 도달 stage를 임시
    scan pose로 채택한다.

## SW Fine Staged Probe: Stage7 도달 성공

- 2026-05-28 12:39 run에서 `root/sw` fine staged MoveJoint가 stage7까지 도달했다.

```text
stage7 target=[138.2 -18.3 125.1 -98.7 94.3 24.9]
AT_SCAN_POSE root/sw joints_deg=[138.2 -18.3 125.1 -98.7 94.3 24.9]
SCANNED_EMPTY root/sw no detection in dwell window
RETURNING_TO_OVERVIEW
SCAN_COMPLETE
```

- 수정:
  - 카메라를 더 가까이 보내기 위해 SW accept stage를 7에서 8로 올렸다.
  - no-arrival 발생 시 마지막 도달 stage를 임시 scan pose로 사용하는 fallback은 유지한다.

## SW Fine Staged Probe: Stage10까지 접근 시도

- 사용자가 stage7 이후 경로가 문제없고 계속 더 가도 된다고 확인했다.
- 수정:
  - SW accept stage를 8에서 10으로 올려 fine staged path의 끝까지 접근을 시도한다.
  - stage8~10 중 no-arrival이 발생하면 마지막 실제 도달 stage를 scan pose로 사용한다.

- 주의:
  - SW `MoveJoint` fallback은 cuRobo의 전체 spline path를 그대로 실행하는
    것이 아니라 endpoint joint move로 우회하는 것이다.
  - 따라서 첫 실기 검증은 저속, clear-space, E-stop 준비 상태에서만 수행한다.

## SW Fallback 1차 실행 결과와 수정

- 실행 결과:
  - slow spline probe도 `success=True` 이후 실제 로봇이 움직이지 않음.
  - `5 s` no-motion 감지 후 MoveJoint fallback을 호출했지만 endpoint에
    도착하지 못하고 `EXEC_TIMEOUT` 발생.
- 해석:
  - spline 명령이 nominal `15 s` 동안 controller queue/busy 상태를 유지해
    fallback MoveJoint가 즉시 유효하게 실행되지 않았을 가능성을 분리해야 함.
- 수정:
  - `root/sw`는 일단 spline probe를 건너뛰고 direct low-speed
    `MoveJoint` endpoint test로 전환.
  - `MoveJoint response`에 target/velocity/acceleration 로그 추가.
- 검증:
  - `py_compile`: 통과
  - `pytest`: `52 passed`
  - `git diff --check`: 통과
  - `colcon build --packages-select strawberry_motion --symlink-install`: 통과
