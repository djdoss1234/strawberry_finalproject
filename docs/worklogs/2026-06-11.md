# 작업 일지 — 2026-06-11

## 목표

실측 TCP 기준으로 수확 좌표계를 재정리하고, marker tray place 경로를 cuRobo 기반으로 검증한다.

---

## 진행 내용

- 플랜지에서 파츠 끝단까지의 실측 길이를 반영해 measured TCP workflow를 추가했다.
- 기존 legacy length compensation과 extra advance가 실제 파지점과 어긋날 수 있음을 기록했다.
- marker place 과정에서 tray-view FK orientation, inclined orientation, clearance search를 시도했다.
- BASE ABS place descent가 위험하거나 부정확할 수 있어 cuRobo Cartesian plan 방식으로 교체를 시도했다.
- unsafe marker place preview 결과를 문서화했다.

---

## 핵심 문제

- marker pose는 잡히지만, place above pose가 IK_FAIL 또는 예기치 않은 관절 branch를 만들 수 있었다.
- 계란판이 로봇에 가까울수록 J1/J4 branch 변화와 tray 충돌 위험이 커졌다.
- measured TCP 기준으로 바뀌면서 기존 SW motion과 좌표 해석이 달라졌다.

---

## 관련 커밋 근거

```text
9159d74 feat: add measured grasp TCP validation workflow
1c074e1 feat: measured TCP real execution validated — adaptive approach + Z bias tuning
d675cff fix: replace BASE ABS place descent with cuRobo Cartesian plan
171f965 fix: enforce safe marker place clearance sequence
a291793 fix: align marker place targets with measured grasp TCP
e25ab09 fix: preserve tray-view FK orientation for marker place
5281998 fix: sample reachable top-down marker place orientations
f99210a fix: search reachable marker place clearances
4e19865 fix: add reach-aware inclined marker place poses
61079ec docs: record unsafe marker place preview result
```

---

## 남은 일

- place target을 단순 티칭 좌표가 아니라 tray frame 기준 slot grid로 일반화한다.
- measured TCP 변경이 SW 수확에 regression을 만들지 않는지 확인한다.
