# 작업 일지 — 2026-06-14

## 목표

티칭한 slot0/1/3 좌표를 이용해 계란판 slot grid를 생성하고, SW 수확 이후 place까지 이어지는 자동화 경로를 검증한다.

---

## 진행 내용

- taught slot0 기준으로 tray slot grid를 생성했다.
- row/column pitch를 반영해 slot2 등 다른 slot 목표를 자동 생성했다.
- unsafe fixed tray TCP realignment를 제거하고 measured TCP grid에 맞게 place target을 보정했다.
- KP1 부근에서 잡기 위해 열린 그리퍼 상태로 BASE -Z 하강 후 close하는 파지 모션을 추가했다.
- place spline 전 operation speed를 100%로 올리는 실험을 했다.
- row2는 30도 tilt와 correction을 적용해 특정 slot place가 맞는 사례가 있었지만, cuRobo descent line deviation 문제가 남았다.

---

## 해결한 문제

- 바로 close하는 방식이 잎/줄기에 밀리는 문제를 줄이기 위해 `open stem descent -> close at KP1 -> pull down` 흐름으로 바꿨다.
- slot0 하나만 쓰는 방식에서 tray grid로 확장할 수 있는 기반을 만들었다.

---

## 관련 커밋 근거

```text
a9e27c3 feat: generate fixed tray slot grid from taught references
8e44fb4 fix: scale MoveLine timeout for reduced robot speed
e9954f0 tune: target local stem midpoint and reduce pick latency
ae488ce docs: record KP1 grasp policy validation
47e1307 fix: align fixed tray place to measured TCP grid
da19f11 revert: remove unsafe fixed tray TCP realignment
1d648f5 feat: descend open gripper to KP1 before grasp
4fa7c86 safety: block release to unverified generated tray slots
039088e feat: calibrate marker tray grid with taught pitch
709edf8 docs: add 2026-06-14 handoff for marker tray grid validation
3c7ea3c feat: enforce operation speed 100% before place spline
63bbd7b docs: update handoff with 2026-06-14 session2 results
```

---

## 남은 일

- row2 place는 Cartesian line constraint 또는 collision geometry 보강 후 재검증한다.
- slot별 place success를 KPI로 반복 측정한다.
