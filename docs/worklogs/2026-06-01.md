# 작업 일지 — 2026-06-01

## 목표

딸기 수확 모션 본격 시작. 룰베이스 수확 80% 목표, 스캔→픽 통합 시퀀스 구현.

---

## KPI 수립

| 항목 | 목표값 | 비고 |
|------|--------|------|
| 전체 사이클 (1개) | ≤ 35초 | 논문 40-50s 대비 개선 |
| 파지 단계 | ≤ 3초 | 논문 5s 대비 개선 |
| 셀 스캔 dwell | 2초 | YOLO 검출 충분 |
| 룰베이스 성공률 | ≥ 80% | 15개 중 12개+ |
| VLA 이관 비율 | ≤ 20% | 가려짐/복잡 케이스 |

모형 딸기: 정상 15개 + 비정상(줄기·잎·기형) 15개 = 총 30개

---

## 시퀀스 설계 변경

기존: Overview → cell → dwell → overview reset → next cell → ... → overview

**신규**: Overview → cell → dwell → [pick all in cell] → next cell (direct, no overview reset) → ... → overview

- 셀 완료 후 HOME → 다음 스캔 포즈 직행 (inter-cell overview 경유 제거)
- pick 통합 시 curobo_planner가 HOME에서 대기하므로 J6 wind-up 없음

---

## 구현 내용

### scan_executor_node.py 수정

- `_detection_poses: List[PoseStamped]` 추가 — 포즈 버퍼링 (기존: 카운트만)
- `_pick_complete_event: threading.Event` 추가
- detection 입력 토픽 변경: `/dsr01/curobo/pick_pose` → `/strawberry/detection/pick_pose`
  - YOLO가 직접 curobo_planner를 트리거하지 않도록 scan_executor가 게이트
- publisher 추가: `/dsr01/curobo/pick_pose` — scan_executor가 1개씩 순차 발행
- subscription 추가: `/dsr01/curobo/pick_complete` — pick 완료 대기
- `_deduplicate_poses`: 30mm 이내 중복 포즈 제거
- `_trigger_picks_for_cell`: TARGET_FOUND 후 포즈 순차 전달 + pick_complete 대기 (timeout 120s)
- 셀 간 overview reset 제거 → `INTER_CELL_DIRECT` 로그만 출력
- `enable_pick_integration` 파라미터 추가 (기본 True)

### curobo_planner_node.py 수정

- `vla_request_pub`: `/strawberry/vla/request` (PoseStamped) 추가
- 파지 실패 시 VLA 토픽 발행 후 HOME 복귀

### harvest_session_logger.py 신규

- `/strawberry/scan/status` 구독 → PICK_TRIGGER/PICK_COMPLETE/PICK_TIMEOUT 파싱
- `/strawberry/vla/request` 구독 → VLA 이관 기록
- 세션별 YAML 저장: `docs/harvest_logs/session_YYYYMMDD_HHMMSS.yaml`
- KPI 실시간 출력: success rate, avg cycle time

---

## 토픽 구조 (업데이트)

```
YOLO detector
  └─→ /strawberry/detection/pick_pose  →  scan_executor_node (버퍼)

scan_executor_node
  ├─→ /dsr01/curobo/pick_pose          →  curobo_planner_node (순차 트리거)
  └─→ /strawberry/scan/status          →  harvest_session_logger, 모니터링

curobo_planner_node
  ├─→ /dsr01/curobo/pick_complete      →  scan_executor_node (완료 신호)
  └─→ /strawberry/vla/request          →  VLA 팀 노드 (파지 실패 이관)
```

---

## VLA 핸드오프 기준 (v1)

| 조건 | 판단 |
|------|------|
| YOLO confidence < 0.5 | VLA |
| 그리퍼 피드백: actual < goal×0.7 (miss) | 재시도 1회 → VLA |
| cuRobo approach 계획 실패 3회 | VLA |
| 이웃 딸기와 거리 < 30mm | VLA |
| 패널 가장자리 10mm 이내 | VLA |

---

## 다음 작업

1. YOLO → `/strawberry/detection/pick_pose` 연결
2. 그리퍼 피드백 구현 (FlangeSerialRead reg 281)
3. 실제 딸기 pick 시도 + KPI 기록
4. MoveIt trajectory 검증

---

## YOLO Seg + Pose 모델 handoff 기록

팀원으로부터 draft 모델 2개를 전달받았다. 아직 완벽한 모델은 아니고 재학습 중이므로,
자동 수확에 바로 연결하지 않고 fusion contract만 기록한다.

로컬 asset:

- segmentation: `/home/user/Downloads/strawberry_seg_best.pt`
- pose: `/home/user/Downloads/strawberry_pose_best.pt`

해석:

- Seg class: `0=ripe`, `1=unripe`, `2=sick`
- Pose keypoint: `KP0=stem_base`, `KP1=stem_mid`, `KP2=stem_tip`

Runtime 원칙:

- 세그 모델과 포즈 모델을 반드시 함께 실행한다.
- pose box 중심이 seg mask 내부에 들어가는 경우만 같은 과실로 매칭한다.
- 매칭된 ripe 객체만 수확 후보로 사용한다.
- unripe/sick, keypoint confidence 부족, mask 면적 이상치, seg/pose 매칭 실패는 수확 금지한다.
- 줄기 방향은 기본 `KP2 - KP0`, fallback `KP2 - KP1`로 계산한다.

남은 일:

1. `config/detection_models.yaml` 기준으로 fusion viewer/node 구현
2. live frame에서 seg/pose 매칭 품질 확인
3. depth + eye-in-hand로 target base pose 계산
4. stem vector 기반 동적 gripper quaternion 계산
5. `/strawberry/detection/pick_pose`, `/strawberry/detection/scene_positions` 발행 연결

---

## 그리퍼 중심 scan pose 재티칭

기존 scan pose는 카메라 중심 기준으로 셀을 잘 보이게 맞춘 상태였다. 하지만 실제 수확에서는
그리퍼가 줄기/과실에 접근해야 하므로, 카메라 중심이 좋아도 그리퍼 접근 방향이 불리하면
후속 pick motion이 어려워진다.

2026-06-01에 네 셀을 모두 다시 티칭했다. 기준은 “카메라가 셀을 보면서도 그리퍼가 셀 중심
작업 영역과 수평으로 접근 가능한 자세”이다.

기록 파일:

- `docs/runs/RUN-20260601-001_gripper_centered_scan_pose_reteach.md`
- `config/scan_pose_candidates_refit_candidate.yaml`

새 버전:

```text
v12_gripper_centered_manual_teach
```

셀별 DART joint pose:

```text
root/nw [144.09, 22.90, -1.00, -238.52, -75.31, 108.68]
root/ne [18.91, 25.97, 1.00, 74.58, 78.17, -115.61]
root/se [22.71, -4.60, 103.56, 97.69, 68.16, -190.82]
root/sw [150.27, -11.92, 109.11, -97.81, 63.33, 10.46]
```

다음 검증:

1. ROS single-cell 실행으로 각 셀 도달/복귀 확인
2. draft YOLO seg+pose 모델로 fusion viewer 확인
3. 검출된 ripe 후보 1개만 저속 pick test
