# 2026-05-27 작업 계획 및 진행 기록

## 오늘 시작 상태

- 전일 display-only camera viewer와 Doosan DART 수동 조작으로
  overview reference pose와 정렬 화면을 확보했습니다.
- 전일 incident 대응으로 viewer의 robot Cartesian control은 철회되고
  fail-closed 상태입니다.
- 오늘은 robot motion 없이 RViz에서 quadtree cell과 물리 workspace의
  방향/형상 대응을 먼저 검증합니다.

## 오늘 목표

1. RViz에서 `cultivation_panel` 기준 네 cell marker를 표시합니다.
2. camera overview의 `NW/NE/SW/SE` 라벨과 RViz label 방향을 비교합니다.
3. `root/nw -> root/ne` next-cell 상태 전환을 화면으로 기록합니다.
4. 이후 `base_link -> cultivation_panel` transform을 정의할 측정 절차를 정합니다.

## 현재 확인한 기준

```text
cultivation_panel origin = 종이 중앙 테이프 교차점
+X = 보드를 정면에서 볼 때 오른쪽
+Y = 보드를 정면에서 볼 때 위쪽

root/nw = (-X, +Y)
root/ne = (+X, +Y)
root/sw = (-X, -Y)
root/se = (+X, -Y)
```

`src/strawberry_motion/exploration/quadtree_map.py`의 quadrant 생성 순서와
`docs/testbed_setup.md`의 축 정의가 위 기준에서 일치함을 확인했습니다.

## 오늘 추가하는 도구와 기록

- `rviz/workspace_exploration.rviz`: cell/label/next marker 캡처용 화면 구성
- `launch/workspace_rviz.launch.py`: marker node와 RViz를 동시에 실행
- `RUN-20260527-001`: RViz와 실제 workspace 방향 대응 검증 절차/결과 기록
- 최초 실행 시 RViz `Global Status: No tf data` 발생:
  marker가 사용하는 `cultivation_panel` frame을 제공하는 TF publisher가
  없었던 것이 원인이므로, standalone visualization용 identity TF
  (`workspace_visualization_world -> cultivation_panel`)를 launch에 추가
- TF 보완 후 축/grid만 보이고 cell marker가 표시되지 않는 증상을 확인:
  node는 `/strawberry/exploration/workspace_cells`를 publish하고 있었지만,
  RViz는 기본 `/visualization_marker_array`를 구독 중이었음
- 원인은 `.rviz`의 MarkerArray topic property를 `Marker Topic`으로
  작성한 설정 오류이며, ROS 2 RViz 형식인 `Topic`으로 수정

## 안전 경계

- 오늘 첫 run은 marker와 RViz 표시만 수행하며 로봇을 움직이지 않습니다.
- `base_link -> cultivation_panel` transform은 아직 측정되지 않았습니다.
- 표시용 identity TF는 실제 robot/base frame registration으로 사용하지 않습니다.
- transform 및 motion safety validation이 완료되기 전에 자동 scan motion을
  실행하지 않습니다.

## 다음 입력/자료

- RViz initial cell 화면 캡처
- `root/nw=SCANNED_EMPTY` 이후 next marker 변화 캡처
- 물리 overview 화면과 RViz 방향 비교 결과

## RViz 실행 확인 결과

- visualization-only identity TF로 `No tf data`를 해소했습니다.
- MarkerArray display의 topic property 오류를 고쳐
  `/strawberry/exploration/workspace_cells` marker publish를 확인했습니다.
- 상태 확인 중 `root/nw=SCANNED_EMPTY` 반영 후 next cell이 `root/ne`로
  갱신된 것을 topic 출력으로 확인했습니다.
- 사용자가 RViz 축과 실제 panel의 `NW/NE/SW/SE` 방향 대응을 확인해
  `RUN-20260527-001`은 방향 기준 `PASS`로 기록합니다.
- RViz screenshot을 전달받아
  `docs/assets/exploration/RUN-20260527-001_rviz_cells.png`로 공개
  evidence에 추가했습니다.

## 다음 단계 결정

- `tcp_base_mm_deg` overview pose는 camera/TCP offset과 depth가 포함되지
  않은 자세 기록이므로 panel transform으로 직접 사용하지 않습니다.
- `base_link -> cultivation_panel` 관계는 panel에 부착한
  AprilTag/ArUco marker와 eye-in-hand transform을 이용해 계산하는
  registration baseline으로 진행합니다.
- 상세 절차: `docs/runs/RUN-20260527-002_panel_frame_registration_plan.md`
