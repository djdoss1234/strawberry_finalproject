# 2026-05-27 작업 계획 및 진행 기록

## 오늘 시작 상태

- 전일 display-only camera viewer와 Doosan DART 수동 조작으로
  overview reference pose와 정렬 화면을 확보했습니다.
- 전일 incident 대응으로 viewer의 robot Cartesian control은 철회되고
  fail-closed 상태입니다.
- 오늘은 robot motion 없이 RViz에서 quadtree cell과 물리 workspace의
  방향/형상 대응을 먼저 검증합니다.

## 오늘 목표

1. RViz에서 `cultivation_panel` 기준 네 cell marker를 표시합니다.
2. camera overview의 `NW/NE/SW/SE` 라벨과 RViz label 방향을 비교합니다.
3. `root/nw -> root/ne` next-cell 상태 전환을 화면으로 기록합니다.
4. 이후 `base_link -> cultivation_panel` transform을 정의할 측정 절차를 정합니다.

## 현재 확인한 기준

```text
cultivation_panel origin = 종이 중앙 테이프 교차점
+X = 보드를 정면에서 볼 때 오른쪽
+Y = 보드를 정면에서 볼 때 위쪽

root/nw = (-X, +Y)
root/ne = (+X, +Y)
root/sw = (-X, -Y)
root/se = (+X, -Y)
```

`src/strawberry_motion/exploration/quadtree_map.py`의 quadrant 생성 순서와
`docs/testbed_setup.md`의 축 정의가 위 기준에서 일치함을 확인했습니다.

## 오늘 추가하는 도구와 기록

- `rviz/workspace_exploration.rviz`: cell/label/next marker 캡처용 화면 구성
- `launch/workspace_rviz.launch.py`: marker node와 RViz를 동시에 실행
- `RUN-20260527-001`: RViz와 실제 workspace 방향 대응 검증 절차/결과 기록
- 최초 실행 시 RViz `Global Status: No tf data` 발생:
  marker가 사용하는 `cultivation_panel` frame을 제공하는 TF publisher가
  없었던 것이 원인이므로, standalone visualization용 identity TF
  (`workspace_visualization_world -> cultivation_panel`)를 launch에 추가
- TF 보완 후 축/grid만 보이고 cell marker가 표시되지 않는 증상을 확인:
  node는 `/strawberry/exploration/workspace_cells`를 publish하고 있었지만,
  RViz는 기본 `/visualization_marker_array`를 구독 중이었음
- 원인은 `.rviz`의 MarkerArray topic property를 `Marker Topic`으로
  작성한 설정 오류이며, ROS 2 RViz 형식인 `Topic`으로 수정

## 안전 경계

- 오늘 첫 run은 marker와 RViz 표시만 수행하며 로봇을 움직이지 않습니다.
- `base_link -> cultivation_panel` transform은 아직 측정되지 않았습니다.
- 표시용 identity TF는 실제 robot/base frame registration으로 사용하지 않습니다.
- transform 및 motion safety validation이 완료되기 전에 자동 scan motion을
  실행하지 않습니다.

## 다음 입력/자료

- RViz initial cell 화면 캡처
- `root/nw=SCANNED_EMPTY` 이후 next marker 변화 캡처
- 물리 overview 화면과 RViz 방향 비교 결과

## RViz 실행 확인 결과

- visualization-only identity TF로 `No tf data`를 해소했습니다.
- MarkerArray display의 topic property 오류를 고쳐
  `/strawberry/exploration/workspace_cells` marker publish를 확인했습니다.
- 상태 확인 중 `root/nw=SCANNED_EMPTY` 반영 후 next cell이 `root/ne`로
  갱신된 것을 topic 출력으로 확인했습니다.
- 사용자가 RViz 축과 실제 panel의 `NW/NE/SW/SE` 방향 대응을 확인해
  `RUN-20260527-001`은 방향 기준 `PASS`로 기록합니다.
- RViz screenshot을 전달받아
  `docs/assets/exploration/RUN-20260527-001_rviz_cells.png`로 공개
  evidence에 추가했습니다.

## 다음 단계 결정

- `tcp_base_mm_deg` overview pose는 camera/TCP offset과 depth가 포함되지
  않은 자세 기록이므로 panel transform으로 직접 사용하지 않습니다.
- `base_link -> cultivation_panel` 관계는 추가 marker 없이 기존
  eye-in-hand calibration, overview pose, 중앙/우측/상측 depth를 이용해
  계산하는 read-only registration baseline으로 진행합니다.
- 상세 절차: `docs/runs/RUN-20260527-002_panel_frame_registration_plan.md`

## Overview Pose 재정렬 기록

- 이전 pose의 `J2=-109.75 deg`는 현재 cuRobo 운용 제한 `-95~+95 deg`를
  벗어나므로 자동 motion 입력으로 사용하지 않습니다.
- DART 수동 조작과 표시 전용 viewer로 중앙 교차점 정렬을 다시 확보했습니다.

```yaml
joint_deg: [101.90, -102.63, 86.28, -13.75, 82.21, -93.33]
tcp_base_mm_deg: [104.97, -301.08, 550.01, 87.01, 66.36, -89.14]
```

- 새 pose도 `J2=-102.63 deg`로 현재 cuRobo 운용 범위 밖이므로,
  `panel_frame_capture`의 read-only TF 후보 계산에만 사용합니다.
- evidence: `docs/assets/exploration/RUN-20260527-002_overview_alignment_revised.png`

## 운용 제한 내 Overview Pose 확보

- 중앙 정렬 상태를 유지하며 `J2`를 현재 cuRobo 운용 제한 안으로 조정한
  최종 관측 자세를 확보했습니다.

```yaml
joint_deg: [97.84, -94.40, 65.95, -10.93, 95.49, -94.79]
tcp_base_mm_deg: [85.91, -385.37, 569.66, 86.05, 67.57, -89.18]
```

- 여섯 joint 모두 현재 운용 제한 범위 안입니다. 다만 이 단계에서는
  자동 scan motion 용도가 아니라 read-only panel TF 측정 기준으로만 사용합니다.
- evidence: `docs/assets/exploration/RUN-20260527-002_overview_alignment_operational_limit.png`

## Panel TF 후보 측정

- 기존 eye-in-hand calibration과 aligned depth를 이용해
  `base_link -> cultivation_panel` 후보를 read-only 방식으로 출력했습니다.

```yaml
translation_m: [0.112833, 0.597764, 0.643978]
rotation_xyzw: [0.701914023, -0.099086927, -0.085965950, 0.700077382]
```

- `config/panel_registration.yaml`에 `candidate_pending_rviz_validation`으로 저장했습니다.
- `workspace_rviz.launch.py`는 identity visualization TF 대신 해당 candidate
  TF를 표시하도록 변경했습니다.
- RViz 확인 전에는 이 값으로 자동 motion을 실행하지 않습니다.

## Registered Panel RViz 표시 확인

- 전달받은 RViz 캡처에서 `base_link` 기준으로 panel outline과
  `NW/NE/SW/SE` cell label이 표시되는 것을 확인했습니다.
- 이는 TF candidate의 ROS/RViz 적용 확인이며, 물리 위치 오차나 자동
  motion 안전성의 정량 검증은 아닙니다.
- evidence: `docs/assets/exploration/RUN-20260527-002_registered_panel_rviz.png`

## Scan Pose Preview 시작

- 각 root cell 중심에 대한 observation pose preview 생성기를 추가합니다.
- 현재 표시용 standoff는 overview pose와 panel TF에서 계산한
  panel-normal 거리 `0.921486 m`입니다.
- preview marker는 RViz 검토용이며 planner/executor에는 연결하지 않습니다.

## Scan Pose Preview 방향 표시 수정

- 첫 RViz preview 캡처에서 노란 arrow가 panel cell에서 camera 후보
  위치로 향해, 관측 pose의 시선 방향과 반대로 표현되는 것을 확인했습니다.
- 실제 motion 연결 전 단계에서 표시 의미를 수정해, 노란 점을 camera
  후보 위치로 표시하고 arrow는 `camera preview -> cell center` 방향으로
  향하도록 변경합니다.
- 이 수정은 시각화 의미 수정이며 robot 명령을 발생시키지 않습니다.
- evidence: `docs/assets/exploration/RUN-20260527-003_scan_pose_preview_direction_issue.png`
