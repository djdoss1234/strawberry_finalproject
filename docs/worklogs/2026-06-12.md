# 작업 일지 — 2026-06-12

## 목표

relocated tray에서 slot0 place를 검증하고, bent stem 수확 시 파지점/접근 방향 문제를 수정한다.

---

## 진행 내용

- joint jog tool에서 BASE TCP pose를 출력하도록 하여 scan pose / slot release pose를 실측 기록할 수 있게 했다.
- taught slot0 place reference를 추가하고 preview/release 흐름을 제한적으로 검증했다.
- slot0 direct transfer와 vertical clearance 접근을 반복 수정했다.
- bent stem에서 기존 KP2 중심 접근이 줄기를 빗겨 잡는 문제를 확인했다.
- local keypoint direction을 사용해 bent stem target을 보정했다.
- gripper close command가 실패하거나 unverified일 때 retry하는 방어 로직을 추가했다.

---

## 관찰된 성과

- slot0 place가 실기에서 관측되었고, TCP 기준 release 좌표가 계란판 slot과 맞는 사례가 있었다.
- 단, tray 위치가 바뀌면 fixed taught pose는 재티칭이 필요하므로 marker/grid 기반 일반화가 필요하다.

---

## 관련 커밋 근거

```text
15cbe76 fix: route taught slot0 place directly from retreat
488677d feat: add taught slot0 place reference preview
dd2837a fix: allow guarded slot0 direct transfer swing
1dd05e2 fix: target bent stems using local keypoint direction
7b6ba62 docs: record harvest sequence and intended stem grasp point
b94589a fix: verify and retry gripper close command
2610fd1 feat: hold after single taught slot0 place
b078d57 feat: print base tcp pose from joint jog tool
d8ad2e1 feat: update relocated tray place pose and limited release
defc5e9 fix: approach taught slot0 from vertical clearance
5c359ed fix: allow guarded slot0 transfer swing
ecc28b2 fix: retry close after unverified gripper state
7d384a8 docs: record first observed slot0 pick and place
```

---

## 남은 일

- slot0 성공을 slot grid 전체로 확장한다.
- 딸기 하단이 tray에 끌리는 문제를 피하려면 above clearance와 release orientation을 다시 조정한다.
